export interface filteredInfo {
  name?: string
  email?: string
  phone?: string
  slogan?: string
  primaryColor?: string
  bgColor?: string
  secondaryColor?: string
  sys?: { id: string }
  partnerPhoto?: { url: string }
}
